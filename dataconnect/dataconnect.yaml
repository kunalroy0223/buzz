specVersion: "v1beta"
serviceId: "quickbuzz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quickbuzz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
